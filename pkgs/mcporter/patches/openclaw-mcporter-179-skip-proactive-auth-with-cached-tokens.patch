diff --git a/src/runtime/oauth.ts b/src/runtime/oauth.ts
index 6038d21..9dac584 100644
--- a/src/runtime/oauth.ts
+++ b/src/runtime/oauth.ts
@@ -236,10 +236,13 @@ async function completeProactiveAuthorization(
   options: Pick<ConnectWithAuthOptions, 'serverName' | 'oauthTimeoutMs' | 'serverUrl' | 'fetchFn'>
 ): Promise<void> {
   if (!options.serverUrl) {
     return;
   }
+  if (await session.provider.tokens?.()) {
+    return;
+  }
   try {
     const result = await sdkAuth(session.provider, {
       serverUrl: options.serverUrl,
       fetchFn: options.fetchFn,
     });
